apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-datasources    # base name, namePrefix will make it dev-grafana-datasources
data:
  datasources.yaml: |
    apiVersion: 1
    datasources:
      - name: Prometheus
        type: prometheus
        access: proxy
        url: http://dev-prometheus.monitoring-dev.svc.cluster.local:9090
        isDefault: true
        editable: false